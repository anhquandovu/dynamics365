//This json file contains configurations for the workspace.
//Change "startupObjectId" and "startupObjectType" values to run the object wanted
{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "al",
            "request": "launch",
            "name": "My server", 
            "server": "https://<vmname>.<azureregion>.cloudapp.azure.com", 
            "serverInstance": "NAV",
            "authentication": "UserPassword",
            "startupObjectId": 22, 
            "startupObjectType": "Page" 
        }
    ]
}

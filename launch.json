//This json file contains the connection information to the service tier
//It includes the server address, authentication needed
//Change "startupObjectId" and "startupObjectType" values to run the object wanted
{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "al",
            "request": "launch",
            "name": "My server", 
            "server": "https://<vmname>.<azureregion>.cloudapp.azure.com", 
            "serverInstance": "NAV",
            "authentication": "UserPassword",
            "startupObjectId": 22, 
            "startupObjectType": "Page" 
        }
    ]
}
